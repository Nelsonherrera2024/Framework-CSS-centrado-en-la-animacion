# Sticky Footer Layout That Always Pins To Bottom

A pure CSS implementation for sticky footer layout that always pins to bottom.

## Usage
Include `style.css` and use the provided classes.

## Features
- CSS only
- Uses EaseMotion variables
- Responsive
