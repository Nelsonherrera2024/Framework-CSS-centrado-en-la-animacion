# ease-particles-bg

Generates floating particle elements that drift within a container with wrapped boundary collision.

**Usage:** Include `style.css` and the inline script from `demo.html`. Configure particle `count` (default 50), size range, color hue, and velocity in the script. Particles bounce off container edges.
