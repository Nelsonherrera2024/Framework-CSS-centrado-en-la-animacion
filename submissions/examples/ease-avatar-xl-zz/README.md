# Avatar XL

## What does this do?
Avatar XL component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="avxl-primary">Avatar XL Primary</div>
```

## Why is it useful?
Reusable avatar xl UI element. Dark theme with CSS custom properties.
