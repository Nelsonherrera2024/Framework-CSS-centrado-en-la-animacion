# Card Dark

## What does this do?
Card Dark component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="ccd-primary">Card Dark Primary</div>
```

## Why is it useful?
Reusable card dark UI element. Dark theme with CSS custom properties.
