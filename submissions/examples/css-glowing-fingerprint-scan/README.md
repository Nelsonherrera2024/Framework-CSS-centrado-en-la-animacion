# CSS Glowing Fingerprint Scan Animation

A futuristic biometric scanning simulation.

## Design
- On hover, the SVG dynamically changes color and adds a `drop-shadow` filter.
- A glowing `scanner-line` fades in and sweeps up and down the container using keyframes.
- Zero JavaScript, purely driven by CSS hover states.
