# Dynamic Scrollspy Reference and Environment Fix (`ease-scrollspy-pb`)

Provides a clean visual example layout for tracking active scroll states alongside an environment-agnostic fixed test routine script.

## What does this do?
Demonstrates interactive navbar tracking mechanics while introducing path fixes via `import.meta.url`.

## How is it used?
```html
<a href="#section1" class="spy-link active-indicator">Overview</a>