# Pure CSS Parallax Scrolling Stars

A breathtaking multi-layer starfield background.

## Concept
- Relies heavily on multiple stacked `box-shadow` values to generate hundreds of "stars" using only 3 HTML elements.
- Each star layer has a different width, height, and animation duration to create a parallax depth effect.
- The `::after` pseudo-elements create a seamless infinite scroll loop.
