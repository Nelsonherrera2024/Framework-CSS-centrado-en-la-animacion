# Button Close

## What does this do?
Button Close component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="bcl-primary">Button Close Primary</div>
```

## Why is it useful?
Reusable button close UI element. Dark theme with CSS custom properties.
