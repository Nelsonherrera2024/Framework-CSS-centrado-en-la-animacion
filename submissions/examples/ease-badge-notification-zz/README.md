# Badge Notification

## What does this do?
Badge Notification component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="bno-primary">Badge Notification Primary</div>
```

## Why is it useful?
Reusable badge notification UI element. Dark theme with CSS custom properties.
