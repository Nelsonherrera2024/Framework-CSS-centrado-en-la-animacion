# Button Dropdown

## What does this do?
Button Dropdown component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="bdr-primary">Button Dropdown Primary</div>
```

## Why is it useful?
Reusable button dropdown UI element. Dark theme with CSS custom properties.
