# Card Actions

## What does this do?
Card Actions component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="cca-primary">Card Actions Primary</div>
```

## Why is it useful?
Reusable card actions UI element. Dark theme with CSS custom properties.
