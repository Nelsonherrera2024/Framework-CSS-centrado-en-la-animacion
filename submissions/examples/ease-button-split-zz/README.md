# Button Split

## What does this do?
Button Split component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="bsp-primary">Button Split Primary</div>
```

## Why is it useful?
Reusable button split UI element. Dark theme with CSS custom properties.
