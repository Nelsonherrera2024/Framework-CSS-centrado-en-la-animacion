# Pure CSS Animated Gradient Border Card

A stylish card featuring a continuously rotating colorful border.

## How it works
- The wrapper contains a hidden overflow and an absolutely positioned `conic-gradient` pseudo-element.
- The `conic-gradient` rotates continuously behind a solid content block, giving the illusion of a glowing border.
- High performance CSS animation.
