# Card Color

## What does this do?
Card Color component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="ccc-primary">Card Color Primary</div>
```

## Why is it useful?
Reusable card color UI element. Dark theme with CSS custom properties.
