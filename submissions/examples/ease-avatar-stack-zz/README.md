# Avatar Stack

## What does this do?
Avatar Stack component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="avk-primary">Avatar Stack Primary</div>
```

## Why is it useful?
Reusable avatar stack UI element. Dark theme with CSS custom properties.
