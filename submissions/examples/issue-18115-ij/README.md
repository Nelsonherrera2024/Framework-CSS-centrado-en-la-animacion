# ease-magnetic-hover

A button that follows the cursor position within its parent area, creating a magnetic attraction effect.

**Usage:** Include `style.css` and the inline script from `demo.html`. The `.magnetic` element translates toward the cursor with a configurable `pull` strength (default 0.3). Resets on `mouseleave`.
