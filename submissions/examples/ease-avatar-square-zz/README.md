# Avatar Square

## What does this do?
Avatar Square component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="avq-primary">Avatar Square Primary</div>
```

## Why is it useful?
Reusable avatar square UI element. Dark theme with CSS custom properties.
