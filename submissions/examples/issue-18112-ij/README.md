# ease-number-countup

Animates a number from 0 to a target value using an ease-out cubic easing function.

**Usage:** Include `style.css` and the inline script from `demo.html`. The `#countup` element displays the animated value. Adjust `target` and `duration` variables in the script.
