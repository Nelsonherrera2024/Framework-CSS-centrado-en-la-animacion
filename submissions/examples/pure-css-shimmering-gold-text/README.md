# Pure CSS Shimmering Gold Text

A luxurious metallic text effect without using images or SVG masking.

## Technique
- Builds a complex multi-stop `linear-gradient` using specific gold hex codes.
- `background-clip: text` paired with `color: transparent` paints the text with the gradient.
- Animating the `background-position` creates a realistic moving light reflection across the metal surface.
