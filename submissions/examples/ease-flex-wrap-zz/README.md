# Flex Wrap

## What does this do?
Flex Wrap component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="flw-primary">Flex Wrap Primary</div>
```

## Why is it useful?
Reusable flex wrap UI element. Dark theme with CSS custom properties.
