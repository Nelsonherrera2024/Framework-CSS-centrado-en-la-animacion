# CSS Lightbox Component
A lightweight, zero-JavaScript lightbox component using CSS `:target` pseudo-class.

- **Mechanism**: Utilizes browser hash-navigation to toggle visibility.
- **Features**: Responsive, accessible, and requires no external framework dependencies.