# Pure CSS Fluid Animated Skeleton Loader

A high-performance layout placeholder component that signals asynchronous loading states without using JavaScript framework overhead.

## How to use
Apply `.em-skeleton` alongside structural primitive blocks like `.em-skeleton-avatar` or `.em-skeleton-line`.
